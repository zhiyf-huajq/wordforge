# Changelog

版本号与 `apk.config.json` 里的 `versionName` 保持一致。日期按工作日誌回填。

## [1.1.1] · 2026-09-19

1.1 发布之后发现的两件事：Release 附件名会被 GitHub 静默改名；APK 其实还没从仓库里拿出去。
顺便把「怎么发版」从口头约定做成了工具 + 文档。

### 发版流程

- 新增 `_dev/release.py`（`npm run release -- 1.1.2`）：打注释 tag → 推 tag → 建 Release → 传附件。
  Release 正文从本文档的 `## [x.y.z]` 段落自动摘取，避免「Release 上说改了三条、CHANGELOG 上写了四条」。
  幂等：Release 已存在就只同步附件。另有 `--dry-run` / `--list` / `--notes-only` / `--sync-notes` / `--force`。
  凭据走 `git credential fill`（复用 Git Credential Manager 已登录的那份），**脚本里不存任何密钥**。
- README 新增「发版（打 tag + 建 Release）」小节，把流程与坑都写进去；顺手补了 `### 打包 APK` 子标题。
- README 的下载入口改指向 Release：两个 `releases/latest/download/…` 地址永远指向最新版，
  以后发版不必再改链接。附件名改成不带版本号的 `wordforge.html` / `wordforge.apk`，理由同上。

### 修掉一个静默坑

- **Release 附件名必须是纯 ASCII。** GitHub 会把附件名里的非 ASCII 字符直接剥掉，
  不报错也不回滚：`词匠-离线背单词.html` 变成 `-.html`，`词匠.apk` 剥成空名后回退成 `default.apk`。
  症状是「HTTP 201、体积和 MIME 都对」—— 只看返回值里的 `size` 会误判成功。
  现在上传后会**回读 `name`** 再判定，中文说明放进 `label`（label 允许中文）。

### 仓库瘦身

- **APK 移出仓库**：`git rm --cached 词匠.apk`，`.gitignore` 加 `*.apk`。
  二进制产物每重新打包一次，就会在 git 历史里永久多留一份副本（删不掉）—— 十几个版本之后仓库就没法看了。
  ⚠️ `git rm --cached` 只影响之后的提交，历史里那份仍在，**克隆体积不会因此变小**；
  真正的收益是「以后不再累积」。
- `dist/` 下的 HTML 产物**反而要留在仓库里**：它是主交付物（clone 下来双击就能用），
  而且 CI 的「产物新鲜度」断言要它入库才能比对。

### 验证

- 新增 `_dev/check-readme.py`（`npm run verify:readme`），并作为 CI 第五步。
  它守的是最容易悄悄坏掉的东西：README 引用了 42 张截图和若干对外下载地址，
  改一次文件名、写错一个 `#锚点`，网页上就是碎图或死链 —— 而 git 不报错、构建也不报错。
  零依赖零出网（只检查引用，不访问外链）。
- 该项检查配了**五项负向对照**（碎图 / 死锚点 / 未闭合标签 / 混入 CRLF / Markdown 图失效），
  逐项确认会变红；并额外确认对照夹具**真的改动了文件** ——
  第一次做的对照里有一项其实是空操作（替换目标写错了），差点被当成「检查通过」。
- CI 四步 → 五步。本机彩排全绿：`git diff` 干净、构建逐字节可复现（MD5 `b58f1d2…`）、
  结构 0 错误 / 0 警告、逻辑 663/663、夹具 41/41、README 体检通过。

## [1.1] · 2026-09-18 → 2026-09-19

09-18 是应用功能，09-19 是仓库工程化。后者不改应用代码，所以不单独占一个版本号。

### 应用功能（2026-09-18）

补上对照市面产品后确认的四个功能缺口。四项都做到「离线可做、不碰数据层」。

- **易混词辨析**（新增 `confuse` 视图）：构建期算出 17,073 组形近词，覆盖 15,909 个词；运行时零计算零请求。
  「练这组」把同组词当一轮专练，干扰项优先从组内取。
- **词汇量测试**（新增 `vocab` 视图）：8 档阶梯自适应，起步第 4 档，18–25 题；
  固定埋 4 个假词陷阱校准虚报。**绝不写学习记录。**
- **考试倒计时反推每日量**：设置页填考试日期 → 今日总览显示「距考试 N 天 · 建议每日 M 词」→ 一键套用。
- **听音辨词 / 例句填空**：复用已有 TTS；无语音或无例句时自动顺延到下一个可做题型的卡片，并说明跳过原因。
- 文章精读新增中国英文媒体源（中国日报五个栏目 + 环球时报英文版）。

### 仓库工程化（2026-09-19，不改应用代码）

- 新增 `.github/workflows/verify.yml`：push / PR 时自动跑「产物新鲜度 + 结构层 + 逻辑层 + 夹具解析」四步。
  产物新鲜度用 `git diff --exit-code` 断言「重建出来的 `dist/` 必须和提交的逐字节一致」，
  挡住「改了 `build/` 里的源码但忘了重新 build」这一类错。不硬写 MD5，产物一变就自动跟着变。
- 新增 `.editorconfig`：把 `* text=auto eol=lf` 的约定延伸到编辑器层面。
  抓下来的夹具（`_dev/_cd_*.html` / `_dev/_gt_*` / `raw/`）显式 `end_of_line = unset`，保持逐字节原样。
- 新增 `package.json`：只放 `scripts` 与 `engines`，**没有任何依赖**。八层验证有了统一入口（`npm run verify`）。
- 新增 `CHANGELOG.md`、`.github/ISSUE_TEMPLATE/bug_report.yml`、`.github/PULL_REQUEST_TEMPLATE.md`。
- 修正 README 里「逻辑」一层的描述：跑它的是 `_dev/_stub_dom.js`（纯 Node 手写假 DOM），**不起浏览器**。
  原文写成「无头浏览器」，会让人以为 CI 要装 Chrome —— 而正因为它是纯 Node，这一层才上得了 CI。
- 修正 `docs/词匠-功能差距分析-20260918.html` 的过期内容：视图数 8 → 10，并把四项已完成的功能
  （词汇量测试 / 考试倒计时反推 / 听音辨义 / 形近词辨析）从「❌ 缺失 P0」改标为「✅ 已落地」；
  「导出词表」由 ❌ 改标 🟡（统计页已有 CSV 导出）。文首加更新横幅，避免被当成待办。
- README 补上全部 42 张界面截图的折叠画廊（此前有 22 张在仓库里但无人引用，引用率 20/42 → 42/42）。
- **开始提供 Releases**：产物（单文件 HTML + 安卓 APK）挂在 Release 附件上分发。
  （APK 真正从仓库里移出、以及附件名必须是纯 ASCII 这两件事，是发布后才补的，记在 1.1.1。）

## [1.0] · 2026-09-17

首个可用版本。

- 单文件 HTML 离线背单词应用：24,027 词条 / 24 本词书 / 927 词根 / 20 字段。
- 视图：今日、学习、复习、测试、精读、词库、统计、设置。
- SM-2 改良记忆算法，11 个参数全部开放可调。
- 文章精读：四档精度（译文 / 生词 / 逐句 / 点词查义）+ 自动生成读后练习 + 完全离线粘贴导入。
- 学习记录只存浏览器 localStorage，不注册、不上传。
- 打包为安卓 APK（`versionCode` 2 / `versionName` 1.1 —— 打包时定的版本号，与本文档的 1.1 对应）。
- 建立八层验证：结构 / 逻辑 / 设置面板 / 精读模块 / 网页解析 / 截图 / 手机窄屏 / APK。
